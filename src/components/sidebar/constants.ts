export const SIDEBAR_WIDTH = 232;
/** Collapsed slot = 10px inset + 8px pad + 32px disc + 8px pad + 10px inset. */
export const SIDEBAR_COLLAPSED_WIDTH = 68;
// Matches the width transition in Sidebar / MainContentWrapper.
export const SIDEBAR_TRANSITION_MS = 480;
/** Labels finish fading before the rail starts moving, and fade back in after it stops. */
export const SIDEBAR_LABEL_MS = 140;
